namespace ApplicationCore.Enums
{
    public enum PostStatus
    {
        DRAFT,
        PUBLISH,
        TRASH
    }

    public enum PostType
    {
        DEFAULT,
        PRODUCT,
        PAGE,
        SOURCE
    }
}
