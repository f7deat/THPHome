import { ModalForm, ModalFormProps } from "@ant-design/pro-components"

const ExamFormModal: React.FC<ModalFormProps> = (props) => {
    return (
        <ModalForm {...props} title="Kỳ thi">

        </ModalForm>
    )
}

export default ExamFormModal